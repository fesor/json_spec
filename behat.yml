default:
    suites:
        default:
            contexts:
                - FeatureContext
                - json_spec
    extensions:
        JsonSpec\Behat\Extension:
            excluded_keys: [id, created_at, updated_at]
            json_directory: './spec/support/files'
